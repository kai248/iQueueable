2. For the queue, you could use a link list implementation. As for the stack, you could use a recursive method where there will just be using the top pointer of the stack. Witht that top pointer alone, you will be able to push elements in, pop elements out as well as retrieve the entire stack.